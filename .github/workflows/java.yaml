name: Java CI

on:
  push:
    branches:
    - '**'
    tags:
    - 'v*-es*'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: anti-social/elasticsearch-plugin-github-action/build@3ef2cc60596f31c5f4cc4daab7551c7eb45c1a03
      with:
        es-version: '7.9'

  release:
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'push' &&
      startsWith(github.ref, 'refs/tags/v')
    needs:
    - build
    permissions:
      contents: write
    steps:
    - uses: anti-social/elasticsearch-plugin-github-action/release@3ef2cc60596f31c5f4cc4daab7551c7eb45c1a03
